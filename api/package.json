{
  "name": "@rcade/api",
  "module": "index.ts",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "exports": {
    ".": {
      "bun": "./index.ts",
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "bun build index.ts --outdir dist --target node && tsc -p tsconfig.build.json --emitDeclarationOnly",
    "prepare": "bun build index.ts --outdir dist --target node && tsc -p tsconfig.build.json --emitDeclarationOnly"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@types/bun": "latest",
    "@types/semver": "^7.7.1"
  },
  "dependencies": {
    "@rcade/input-classic": "workspace:*",
    "semver": "^7.7.3",
    "smol-toml": "^1.5.2"
  },
  "peerDependencies": {
    "typescript": "^5.0.0",
    "zod": "^4.1.13"
  }
}