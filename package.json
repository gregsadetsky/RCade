{
  "name": "rcade-monorepo",
  "private": true,
  "workspaces": ["web", "client", "cli", "create", "action-deploy"],
  "scripts": {
    "dev:web": "bun run --cwd web dev",
    "dev:client": "bun run --cwd client dev",
    "build:cli": "bun run --cwd cli build",
    "check": "bun run --cwd web check"
  }
}
